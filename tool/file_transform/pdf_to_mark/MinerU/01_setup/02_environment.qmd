# environment

```{bash}
micromamba activate -p .CondaPkg/.pixi/envs/default # run CIL in current environment.
# install the model firstly.
export MINERU_MODEL_SOURCE=modelscope
mineru -p ./input/pdf1.pdf -o ./output/mark1/ -b pipeline
```
