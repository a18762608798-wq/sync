# 列空间和零空间

## 列空间

可定义: 

$$
rank(A) = \dim R(A)
$$
其中 R(A) 表示列空间。

如果 $Ax = b$, 也就是b可以由A线性组合，则有：

$$
b \in R(A)
$$

对于矩阵也可，比如 $A^TA$ 显然可以被 $A^T$ 线性组合:

$$
R(A^TA) \in R(A^T)
$$

## 零空间

如 $Ax = 0$，则:

$$
x \in N(A)
$$

#### 2.1.2.3 Rank-Nullity Theorem

$$
dim(R(A)) = n - dim(N(A))
$$
