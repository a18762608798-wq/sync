# 问导师提问清单 (PRA Regular, 以 Yu2023 模型 + 03 路线为准)

> 日期：2026-09-16；已定事项不再问：旧模型不进正文、正文只写 03 ansatz、
> 下一步是 $s,\delta$ 全扫描 + qmeas 单比特旋转测量、机型随校准切换。
> 本清单只问三件事：PRA 格式、物理量展示结构、章节内容分配。

## A. PRA 格式：先单栏还是同步双栏

背景：REVTeX 投稿送审用 `preprint` 单栏即可，终稿前需切 `reprint` 双栏检查长度；
当前 `paper/main.tex` 用的是 `reprint`。

1. **是否暂且以 `preprint` 单栏为主推进，投前再切 `reprint` 检查篇幅？** 还是现在就维护双栏版本？

## B. 物理量展示结构：$Z_R$ 与两个新量在模拟机 / 真机上怎么摆

背景：$Z_R$ 标准但硬件难测，AFM 结构因子和 String Operator 库中尚无定义；
下一步硬件是 $s,\delta$ 全扫描 + qmeas 单比特旋转测量。

1. **模拟机部分是否这样摆：$Z_R$ 相图做数值参考 + AFM/String 相图并排叠边界，证明三者复现同一相界？** 还是 $Z_R$ 只在引言/方法提一句，不占图幅？
2. **硬件部分是否这样摆：一张 $(s,\delta)$ 全扫描热力图 (AFM/String) 为主 + 若干固定切线 (如 $\delta=1$ 扫 $s$) 叠数值边界为辅？** 误差棒是否只来自 qmeas 统计 + 缓解后传播，不做额外标定图？

## C. PRA 章节内容分配

建议骨架 (Regular)：引言 → 模型 → 方法 (变分 ansatz + 测量 + 误差缓解) → 结果 (数值相图 → 硬件能量 → 硬件拓扑读出) → 讨论展望 → 附录。

1. **引言定位同意吗？** SSH 拓扑 + NISQ 变分 + 误差缓解三线收束，claim 落在“云端硬件能量百分之几精度 + AFM/String 硬件分相”？
2. **方法节篇幅怎么分？** 变分 ansatz (03 even/odd 单层)、qmeas 分组测量、readout/ZNE/rZNE 三段，是否都放正文，还是把 ZNE 拟合初值、删零角度门下界 $5.1\times10^{-4}$ 踢进附录？
3. **结果节四小节是否保留？** 数值相图与边界、约束验证 (可压缩)、硬件能量 rZNE、硬件拓扑读出 (待补)。约束验证是否压成一段或进 SM？
4. **01 (Bézier) / 02 (直线+环形) 是否正文只用一句话交代演变，细节全进附录/SM？**
