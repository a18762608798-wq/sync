# README

* 依赖参考 `CondaPkg.toml` 和 `Project.toml`. 运行需要至少克隆 `adiabatic_evolution/` 整个文件夹.

* `main.jl` 负责运行计算(也是修改参数的地方)， `plot.py` 负责绘图.

* 数据在 `data/`, 图表在 `pic/`.

* `pic/` 所示并不完全正确，理由参考 `./pic/warning.md`.
