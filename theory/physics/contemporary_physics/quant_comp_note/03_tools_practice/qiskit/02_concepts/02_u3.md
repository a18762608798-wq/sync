# u3

The u3 gate in qiskit been defined to,

$$
U(\theta, \phi, \lambda) = P(\phi) R_Y(\theta) P(\lambda) = e^{i(\phi + \lambda)/2}R_Z(\phi)R_Y(\theta)R_Z(\lambda)
$$

所以有：

$$
U3(θ,ϕ,λ)† = U3(−θ,−λ,−ϕ)
$$

**这些变换是主动变换，神了**, 也就是和我们所说经常反过来. 它喜欢 $UFU^\dagger$, 这导致实际上对Z旋转的时候无效角是 $\lambda$
