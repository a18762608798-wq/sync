# Time Reversal

## Classical Shadow

where 

$$
u_T = \bigotimes\limits_{i\in I_1} \sigma_i^y, 
$$

$T_1$ is a partial transpose of part 1, therefore we just need to proof

$$
tr(\mathbb E_{U', s'}[\widetilde\rho'] u_T \mathbb E_{U,s}[\widetilde\rho^{T_1}]u_T^\dagger) = Z_T.
$$

Viz., proof 

$$
\mathbb E_{U, s}[\widetilde\rho^{T_1}] = \rho^{T_1}
$$

In fact, $T_1$ is a Linear mapping, the relationship is establishment evidently,

$$
\mathbb E_{U, s}[T_1 [\widetilde \rho]] =T_1[\mathbb E_{U, s}[\widetilde \rho]].
$$

The specific linear mapping form ref to [[TR_T_1_Tensor_Multipulication]].