# 旋转算符

我们不需要对类似

$$
R_x(\theta) Y R_x(-\theta)
$$

的结果做一般的算符展开计算。

因为其证明在旋转算符的定义中已经写出；其是绕 $n$ 的参数空间旋转；单比特的泡利向量投影 $\sigma_n$ 的参数是三，而单比特旋转基于泡利向量投影 $\sigma_{n'}$ (当然 $n$ $n'$ 一般参数取值不同)。

所以直接选同一个参数空间表示 $\sigma_n$ 和 $\sigma_{n'}$ ，旋转就是 $\sigma_n$ 在参数空间内对 $\sigma_{n'}$ 轴的旋转。
