# assert

- **`raise`** → **已考虑到的、可预期的错误**：你有意识地触发它，并且通常会配套写 `try/except` 分支去处理（重试、降级、给用户友好提示）。

- **`assert`** → **意外的、不该发生的情况**：理论上永远不该失败，失败 = 代码有 bug，说明你的逻辑假设被打破了，`assert` 只是让 bug 尽早暴露出来，方便定位。

```{python}
a = 1
assert a == 2, "a must be 1"
```
