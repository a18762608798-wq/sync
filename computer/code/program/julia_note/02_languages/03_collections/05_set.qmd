# set

> **Set 对于元素查找和删除的时间复杂度为 O(1)，而对于数组则为 O(n)** 。

```{julia}
# definition
set1 = Set([1, 2, 3])
set2 = Set(1:1:4)
```

```{julia}
# operations
1 in set1
intersect(set1, set2) # 交集
union(set1, set2) # 并集
setdiff(set2, set1) # 差集
```
